<!DOCTYPE html>
<html>

<head>
  <% include partials/head %>
  <script src="/javascripts/mypage.js"></script>
</head>

<body>
  <header>
    <% include partials/header %>
    <% include partials/breadcrumb %>
  </header>
  <main id="main">
    <img class="introImageBoard" src="/images/background2.jpg" />
    <div class="ui container">
        <form class="ui form">
            <div class="field">
              <label>이름</label>
              <input type="text" name="name" placeholder="이름" id="form_name" value="<%= info.name %>" readonly>
            </div>
            <div class="field">
              <label>비밀번호</label>
              <input type="password" name="password" placeholder="비밀번호" id="form_password">
            </div>
            <div class="field">
              <label>전화번호</label>
              <input type="text" name="phone" placeholder="전화번호" id="form_phone" value="<%= info.phone %>" readonly>
            </div>
            <div class="field">
              <label>입학년도</label>
              <input type="text" name="year" placeholder="입학년도" id="form_year" value="<%= info.year %>" readonly>
            </div>
            <div class="field">
              <label>학과</label>
              <input type="text" name="major" placeholder="학과" id="form_major" value="<%= info.major %>" readonly>
            </div>
          </form>
          <button class="fluid ui primary button" style="margin-top : 30px;" id="saveButton">Save</button>
    </div>
  </main>
  <footer>
    <% include partials/footer %>
  </footer>
</body>

</html>